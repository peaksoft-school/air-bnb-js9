import { createSlice } from '@reduxjs/toolkit'

const initialState = {
   anouncementData: [],
}

export const anouncementSlice = createSlice({
   name: 'anouncement',
   initialState,
   reducers: {},
})
